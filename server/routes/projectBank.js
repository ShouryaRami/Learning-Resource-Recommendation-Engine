/**
 * @desc Project Bank routes
 * Handles instructor project idea creation and
 * student browse and adopt flow.
 * Full implementation in upcoming commit.
 */
const express = require('express')
const router = express.Router()

module.exports = router
